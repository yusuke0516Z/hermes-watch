# Hermès Bag Alert 👜

Hermès日本公式オンラインショップのバッグ在庫を監視し、**新商品・再入荷・新カラー・新サイズ・価格変更**を検知してLINEとメールで通知するツール。

GitHub Actions で **24時間**動く（深夜は5分・日中は15分間隔。正本は `config.json` の `loop`）。個人の在庫確認を自動化する目的で、リクエストは1回につきカテゴリページ1枚のみ。

> 📌 **設定に個人情報を書かないこと。** 宛先メール・トークン類はすべて環境変数（ローカルは `.env`、CIは GitHub Secrets）から読む。

## 何を監視しているか

`config.json` の `watch_models` で指定したモデル。現在は **Picotin / Constance / Lindy / Garden Party**（2026-08-23 ガーデンパーティ追加）。

### 監視対象を追加する手順（3ステップ・省略不可）

1. **公式サイトの商品名をそのまま** `config.json` の `keywords` に入れる
2. `hermes_monitor.py` の `SELF_TEST_FIXTURES` にその公式商品名を1件足す
3. `python3 hermes_monitor.py --self-test` が通ることを確認してから push

```json
{ "model": "Kelly", "keywords": ["ケリー", "kelly"] }
```

> ⚠️ **英語キーワードは飾りで、一致するのは日本語だけ。**
> 日本サイトは `slug` も `url` もパーセントエンコードされた日本語（`バッグ-《ピコタン・ロック》-18`）で、
> 米国サイトのような英語スラッグが存在しない。`"kelly"` はどこにも一致しない。
>
> ⚠️ **公式名は中黒（・）で区切られる。** 「バッグ 《ガーデン・パーティ》 36」「バッグ 《ピコタン・ロック》 18」。
> 中黒・空白・長音符は `_norm()` が正規化して吸収するが、**必ず公式表記を書くこと**。
>
> **2026-08-23の事故**: 「ガーデンパーティ」（中黒なし）＋英語2語で登録したため公式名に一致せず、
> ガーデンパーティは**一度も監視されていなかった**。しかもログは `matched:0` で「在庫なし」と
> 区別がつかず、気づく手段が無かった。→ 上の手順3（`--self-test`）とCIステップで再発を防ぐ。

`priority_colors` に載っている色（Etoupe・Gold・Noir 等）が出ると通知の件名に ⭐⭐⭐ が付く。

> ⚠️ **2026-08-23実測: この ⭐ は現在1件も点かない。** カテゴリJSONの色は `avgColor` で、
> 返るのは総称色（ベージュ/ナチュラル・ブラック・グリーン・ブルー・グレー・マルチカラー・オレンジ・マロン）だけ。
> エトゥープ／ゴールド等のエルメス色名は商品詳細ページにしかなく、そこはDataDomeでブロックされ取得できない。
> **通知の可否には影響しない**（送るかどうかは `watch_models` だけで決まる）。総称色で妥協するか撤去するかは未決。

## 仕組み

```
Hermèsカテゴリページ（レディスバッグ）
  └─ HTML内の <script id="hermes-state"> にSSRの商品JSONが埋まっている
       → sku / title / avgColor / size / price / 画像URL / stock.ecom が取れる
  └─ 一覧は「在庫あり商品のみ」掲載（クエリに has_stock フィルタ）
       → SKUの出現 ＝ 買える状態になった（新商品 or 再入荷）
       → SKUの消滅 ＝ 売り切れ
  └─ state/products.json と差分を取り、変化があった時だけ通知（重複通知を防ぐ）
```

### 調査で確定した制約（2026-08-12 実測）

| 経路 | 結果 |
|---|---|
| **カテゴリページ** | ✅ 素朴なGETで取得可。UA不問（curl/Googlebot/UA無しでも通る）。GitHub ActionsのIPからも通過を確認 |
| 検索ページ `/search/?s=...` | ❌ DataDomeブロック |
| 商品詳細ページ | ❌ Cloudflareブロック（**素材はメールに載せられない** → 商品ページリンクで代替） |
| バックエンドAPI `bck.hermes.com` | ❌ ブロック |

### ⚠️ 鮮度の限界（重要）

カテゴリページは Cloudflare CDN で **`max-age=3600`（最大1時間）キャッシュ**される。キャッシュを外す方法（URLにクエリ付与）は DataDome が即ブロックするため、**本物のブラウザでも新鮮なデータを取得できない**。

つまり **検知の遅延はポーリング間隔ではなくHermès側のキャッシュで決まる**。5分間隔にしても2分間隔にしても、エッジが同じキャッシュを返す限り差は出ない。

**23.3時間・99回の実測結果（2026-08-13）**：

| 指標 | 実測値 |
|---|---|
| データの古さ（`age`） | 最小0分 / **中央31分** / 最大60分 |
| オリジンの実更新間隔 | 最短29分 / **中央60分** / 最長146分 |

→ **平均31分・最悪1時間遅れて気づく**ことになる。人気色が数分で消える場合は取り逃す。

最新の実測は随時確認できる：

```bash
python3 hermes_monitor.py --freshness-report
```

#### 鮮度を上げる手段（実測で分かったこと）

**❌ 効かないと確認済み**

| 試したこと | 結果 |
|---|---|
| ポーリング間隔を詰める（2分・5分） | 同じキャッシュを見るだけで無意味 |
| URLにクエリを付けてキャッシュ回避 | `BYPASS` になるがDataDomeが即ブロック |
| パス変種（`//`・`/./`・`/index.html`） | 前2つは同じキャッシュに正規化。最後はブロック |
| 本物のブラウザで直接アクセス | JSチャレンジが解けず取得不可 |
| 住宅用プロキシで同じURLを叩く | **キャッシュはCDN側で共有されるため無意味**（誰が叩いても同じ古いコピー） |

**✅ 効く手段（実装済み）**

1. **複数カテゴリページの併用（無料）**
   各ページのCDNキャッシュは**独立に期限切れする**（実測: 46分/42分/37分/33分とバラバラ）。
   同じ商品を含むページを複数見て和集合を取れば、どれかが先に更新される分だけ実効遅延が縮む。
   `category_urls` に2件設定済み。**追加できるのは全件掲載（`total <= items`）のページのみ** —
   広いカテゴリは48件で打ち切られるため監視に使えない。

2. **ScrapFly経由（有料・`scrapfly.enabled`）**
   本質は住宅IPではなく **キャッシュを外せること**。クエリ付きURLで `BYPASS` にしてオリジンまで
   届かせ、DataDomeを `asp` で突破する。これが唯一リアルタイムに近づく方法。

   ```bash
   # 課金前に、本当に鮮度が上がるか実証する
   python3 hermes_monitor.py --test-scrapfly
   ```

   **実証済み（2026-08-13）**:

   | | 無料経路 | 有料経路 |
   |---|---|---|
   | cf-cache-status | HIT | **MISS** |
   | データの古さ | 54分前 | **0分（リアルタイム）** |
   | 商品取得 | 21件 | 21件 |

   実コストは **30クレジット/回**（住宅IP 25 + JS描画 5。`asp` が描画を伴うため
   `render_js: false` でも描画分の5クレジットは発生する）。

   運用設定（2026-08-13 $30プラン契約後）: **全時間帯を有料経路・24時間リアルタイム監視**。
   間隔・時間帯・予算・クレジット試算の**正本は `config.json` の `loop` セクション**
   （GitHubが高頻度cronを間引くため、cronは約30分ごとの起動トリガーに格下げし、
   実際の監視間隔は `--loop` がプロセス内で管理する）。間隔を詰める時は必ず
   `loop._cost_note` の試算をやり直すこと。

   有料経路が落ちても（クレジット切れ含む）**自動的に無料経路へフォールバック**するので
   監視は止まらない。フォールバックが起きた時は**LINEに1回だけ警告**が届く
   （「検知が最大1時間遅くなります」）。回復すると次のフォールバックでまた1回通知される。

## セットアップ

### 1. LINE通知（推奨・移動中でも気づける）

> ⚠️ **2024年9月に作成フローが変わった。** 古い記事の「LINE Developersで直接チャネル作成」は現在使えない。
> 先に**LINE公式アカウント**を作り、そこからMessaging APIを有効化する。

1. [LINE Official Account Manager](https://manager.line.biz/) にLINEアカウントでログイン → アカウントを作成
2. 「設定」→「Messaging API」→ **「Messaging APIを利用する」** をクリック
   （プロバイダー名は**後から変更できない**ので慎重に）
3. [LINE Developers](https://developers.line.biz/console/) を開く → 該当チャネル → 「Messaging API設定」タブ
   → 最下部の **チャネルアクセストークン（長期）** を発行してコピー（正本は1Passwordへ）
4. manager.line.biz の「応答設定」で **応答メッセージ・あいさつメッセージをオフ**（静かにする）
5. 通知を受け取る人が **Botを友だち追加**（`https://line.me/R/ti/p/@ベーシックID`）
   **追加していない人には届かない**ので必須
6. `.env` に設定し、`config.json` の `line.enabled` を `true` に：

   ```bash
   LINE_CHANNEL_TOKEN=発行したトークン
   ```

7. テスト送信： `python3 hermes_monitor.py --test-line`

#### リッチメニュー（トーク画面下部のメニュー）

```bash
python3 setup_richmenu.py          # 作成・画像アップロード・全員に適用
python3 setup_richmenu.py --list   # 登録状況の確認
python3 setup_richmenu.py --clean  # 全削除
```

画像は `assets/richmenu.html` を1250×843で撮影して `assets/richmenu.png`（2500×1686）にしたもの。
デザインを変えたい時はHTMLを編集 → 同じサイズで撮り直し → `setup_richmenu.py` を再実行。

> リンク先には **クエリ付きURL（`?facet_line=...` 等）を使わないこと。** DataDomeにブロックされ、
> タップした人にキャプチャ画面が出る。プレーンなカテゴリURLのみ採用している。

#### アカウントのプロフィール画像

`assets/icon.png`（640×640）を用意してあるが、**プロフィール画像はAPIで設定できない**。
manager.line.biz →「設定」→「アカウント設定」から手動でアップロードする。

### 2. メール通知（任意）

**LINEが主経路。メールは無くても動く。** 使わない場合は `config.json` の `smtp.enabled` を
`false` のままにするか、`HERMES_SMTP_PASSWORD` を設定しなければ自動的にスキップされる。

> 障害アラート（12回連続で取得失敗）は**LINEにも流れる**ので、メールを使わなくても
> システムが止まったことに気づける。

1. 送信元にするGmailで [アプリパスワード](https://myaccount.google.com/apppasswords) を発行
   （2段階認証が未設定なら先に[セキュリティ設定](https://myaccount.google.com/security)から有効化）
2. `.env` に記入：
   ```
   HERMES_SMTP_USER=送信元@gmail.com
   HERMES_SMTP_PASSWORD=アプリパスワード16文字（スペースなし）
   HERMES_RECIPIENT=通知先@gmail.com
   HERMES_FAILURE_ALERT_TO=障害警告の宛先@gmail.com
   ```
3. テスト送信： `python3 hermes_monitor.py --test-email`
4. 受信側のGmailで、`Hermès Watch` からのメールに**フィルタ＋スター＋重要マーク**を設定しておくと埋もれにくい

### 3. GitHub Actions（24時間監視）

リポジトリの Settings → Secrets and variables → Actions に登録：

| Secret | 内容 |
|---|---|
| `LINE_CHANNEL_TOKEN` | LINEチャネルアクセストークン |
| `HERMES_SMTP_USER` | 送信元Gmailアドレス |
| `HERMES_SMTP_PASSWORD` | Gmailアプリパスワード |
| `HERMES_RECIPIENT` | 通知先メールアドレス |
| `HERMES_FAILURE_ALERT_TO` | 障害警告の宛先 |

登録すれば自動監視が始まる（起動は約30分ごと・各起動が `--loop` で予算時間ぶん監視）。手動実行は Actions タブ → `hermes-monitor` → Run workflow（dry_run にチェックで1回だけ安全に確認）。

## 運用コマンド

```bash
# 監視キーワードが公式商品名に一致するか検証（通信なし・config変更後は必須）
python3 hermes_monitor.py --self-test

# 1回だけ実行（通知を送らず内容だけ確認・stateは保存されない）
python3 hermes_monitor.py --once --dry-run --no-jitter

# 通知テスト
python3 hermes_monitor.py --test-line
python3 hermes_monitor.py --test-email

# キャッシュ鮮度の実測レポート
python3 hermes_monitor.py --freshness-report

# ログ
tail -20 logs/monitor.log
```

### ローカルのlaunchd（2026-08-23に停止・原則ロードしない）

`com.tatsumi.hermes-bag-alert.plist` を `~/Library/LaunchAgents/` に置いて `launchctl load` すると5分間隔で動くが、
**GitHub Actionsと二重稼働になるため通常はロードしない**。2026-08-23に `launchctl unload -w` で永続無効化した
（`-w` 付きなので再ログインでも復活しない）。

止めた理由（すべて実測）:

- **同じ商品で奥様に2通届いていた。** 8/23は ローカル18:49「Lindy 新カラー」／CI 19:04「Lindy 再入荷！」。
  stateを別々に持つのでイベント種別まで食い違う（README冒頭の設計どおり「最大2通」が実際に起きていた）
- **ScrapFlyクレジットが枠を超えていた。** 8/16〜8/23のローカル実行は1,068回 ≒ 32,040クレジット（当月消費74,288の約4割）。
  healthcheckの月末着地予測は 225,250 / 200,000 で超過見込みだった
- Macがスリープすると止まるので、そもそも24時間監視の担い手にはならない

再開する場合は `launchctl load -w ~/Library/LaunchAgents/com.tatsumi.hermes-bag-alert.plist`。
その際は**クレジット試算とLINE重複を必ず再検討する**こと。

## 障害時の動き

- 取得失敗（ブロック・ネットワーク断）はログに記録して静かに終了（通知を乱発しない）
- **12回連続失敗で `HERMES_FAILURE_ALERT_TO` に警告メール1通**
- 通知が全経路失敗した場合は state を保存せず終了 → 次回に同じイベントを再検知して再送
- LINEとメールは**独立して送信**するので、片方が落ちても他方は届く

## ファイル構成

| ファイル | 役割 |
|---|---|
| `hermes_monitor.py` | 本体（取得・差分判定・通知） |
| `config.json` | 監視モデル・優先カラー（**個人情報は書かない**） |
| `setup_richmenu.py` | LINEリッチメニューの作成・適用 |
| `assets/icon.html` / `icon.png` | アカウントアイコン（640×640・手動アップロード用） |
| `assets/richmenu.html` / `richmenu.png` | リッチメニュー画像（2500×1686） |
| `state/products.json` | 既知SKUと在庫状態。重複通知防止の要 |
| `logs/freshness.jsonl` | キャッシュ鮮度の実測データ |
| `.github/workflows/monitor.yml` | 24時間監視のスケジュール |

## 免責

個人の在庫確認を自動化する目的のツール。Hermès に過剰な負荷をかけないよう、1回の実行につきカテゴリページを1枚取得するだけに留めている。転売・買い占め等の用途は想定していない。

## 改善提案（自動点検より）

### 2026-08-14: cron間隔劣化が同日の対策後も解消していない

**確認した事実**（直近24時間、GitHub Actions run一覧と `logs/freshness.jsonl` を突合）:

- 同日昼（JST 11:43〜11:59）に `ef10f96`／`24dc9dc` で「30分cron×28分ループ」対策を投入済み。
- 対策**後**の scheduled run 間隔（成功run同士、`workflow_dispatch` の手動実行は除く）: 47〜116分
  （目標は30分）。`logs/freshness.jsonl` の `checked_at` ギャップも同期間で最大約116分。
- 対策前にコミットメッセージで報告されていた劣化幅（45〜65分）と同水準か、それ以上。対策の効果が
  実測で確認できていない。
- 失敗run（Aug13 14:42 UTC、run #27）は1件のみで、これは `ef10f96` の説明どおり state push時の
  GitHub側一時500が原因（監視自体は成功）と既に特定済み。今回の劣化とは別件。

**原因の切り分け**: 問題は2つ重なっていた。

1. **ループ予算の使い残し**: `run_loop` は「次のイテレーションが予算内に収まらなければ始めない」
   仕様のため、`budget_seconds=1680`（28分）・`day_interval_sec=900` では昼間は必ず2回
   （0分・15分）で打ち切られ、**28分の予算のうち実質15.8分しか監視に使えていなかった**。
2. **cronトリガー自体の遅延**: 残りの穴はGitHub Actionsの `schedule` の遅延・間引きによるもの。
   `:13,:43` へのオフセットでは解消していない。

この2つが乗算で効き、**実測32回/日＝設計168回/日の19%**しか監視できていなかった。
同時にScrapFlyのクレジット消費も月約28,200と、**200,000の枠の14%しか使えていなかった**
（＝間隔を1秒も狭めずにカバレッジを5倍にできる余地が丸ごと残っていた）。

**対応（同日実施・コミット参照）**: 「cronの正確さに依存しない」設計に変更した。

- `loop.budget_seconds` を 1680 → **10200秒（170分）** に延長（`monitor.yml` の
  `timeout-minutes` も 35 → 185 に追随）
- cronは `:13,:43` のまま高頻度に残す＝起動の取りこぼしに対する保険
- 走行中に発火したcronは `concurrency: cancel-in-progress: false` により pending で待機し、
  走行中のrunが終わった瞬間に起動する → **実質連続監視**になり、cronが遅れても間引かれても穴が出ない
- runが異常終了しても次のcron（最大30分後）が拾うため自己修復する

**監視間隔（夜5分／昼15分）は変更していない。** 変わったのは「設計どおりの間隔を実際に達成できるか」
だけで、クレジットは設計値の月151,200（枠の76%）に収まる。予算を170分に留めたのは、
ジョブ上限6時間に余裕を残しつつ、ランナー障害時に失う state を3時間以内に抑えるため
（state は現状ジョブ末尾でしか push されないため、run を延ばすほど重複通知のリスクが伸びる）。

**さらに先の選択肢**（今回は見送り。必要になったら検討）:

1. `run_loop` 内から state 変化時に commit＆push する。これを入れれば run 長を6時間まで
   伸ばしても重複通知リスクが増えない。
2. 常駐ホスト（Oracle Cloud Always Free / Fly.io / 自宅Raspberry Pi等）で `--loop` を回し、
   GitHub Actionsは死活監視・バックアップに格下げする。cron依存・6時間上限・state push問題が
   すべて消える構造的な解。
3. 外部cronサービス（Cloudflare Workers Cron等）から `workflow_dispatch` を叩く。
   GitHub製cronの不安定さだけを外せるが、PATの保管が必要。

### 2026-08-27: cron間隔が単発で454分（約7.6時間）まで劣化（自己解消済み）

**確認した事実**（直近24時間、GitHub Actions run一覧と `logs/freshness.jsonl` を突合）:

- run #330（2026-08-26 23:26〜2026-08-27 02:02 UTC、正常完走・成功）の直後、次の run #331 が
  始まったのは 2026-08-27 09:35:36 UTC で、その間 **約7時間半、scheduled run が1件も
  （cancelled含め）発生していない**。
- 同じ期間の `logs/freshness.jsonl` の `checked_at` ギャップも同じく454分
  （2026-08-27 11:02→18:36 JST）で、2026-08-14/15の170分ループ化対策後では過去最大
  （次点は同日07:10→08:27の77分。対策直前の記録上の最大は168分）。
- 直近24時間で failure/timed_out な run は0件。ScrapFly経由比率は127件中125件（98.4%）で健全。
- run #330 自体は通常の所要時間（約155分・予算170分以内）で正常終了しており、state push も
  成功。この経路にコード起因の異常は見当たらない。

**原因の切り分け**: cron (`13 * * * *`, UTC) は 03:13〜08:13 の間に6回発火するはずだが、
この間 run 一覧に1件も現れていない（cancelled ですら無い）。`cancel-in-progress: false` の
pending キューが機能していれば、走行中でなくても遅くとも次のcron発火時には新runが立つはずで、
6回連続で「キューにすら乗らない」のは、2026-08-14の改善提案で既に特定した「GitHub側 schedule
トリガーの遅延・間引き」と同じ系統の問題が、今回は通常（数十分規模）より大きく数時間規模で
出たものと判断した。`config.json` / `monitor.yml` / `hermes_monitor.py` 側に今回特有の不具合は
見当たらない。

**対応**: 制約（監視間隔を狭めない・大規模な設計変更をしない）の範囲で安全に直せるコード修正が
無いため、コードは変更していない。単発の事象であり、直後（07:10〜08:27に一度77分のギャップは
あったが）から現在まで通常の約15分間隔に復帰しているため、緊急の追加対応は不要と判断した。
2026-08-14の改善提案に記載済みの選択肢（常駐実行への移行 等）が、今回のような数時間規模の
劣化に対する根本対策として引き続き有効な候補。次回以降も同規模の劣化が繰り返す場合は、
常駐実行への移行を再検討する必要がある。

### 2026-08-28: cron間隔劣化が3日連続で発生・今回は点検時点でまだ進行中

**確認した事実**（直近24時間、GitHub Actions run一覧と `logs/freshness.jsonl` を突合、点検時刻 2026-08-28 12:35 UTC）:

- run #333（2026-08-28 05:20:22〜08:05:59 UTC、正常完走・成功、ScrapFly比率も健全）を最後に、
  次の run が**点検時点で1件も（cancelledですら）発生していない**。cron (`13 * * * *`, UTC) は
  この間 09:13/10:13/11:13/12:13 の**4回発火するはずが1回もキューに乗っていない**。
- `logs/freshness.jsonl` も同時に止まっており、最後の記録（08:05:52 UTC）から点検時点まで
  **約270分（4.5時間）が経過中、かつ延伸中**。直近24時間の最大ギャップも372分（約6.2時間）。
  いずれも45分の異常しきい値を大きく超過。
- 直近24時間の failure/timed_out run は0件、ScrapFly経由比率は97.8%（46件中45件）で健全。
  run #333自体にもコード起因の異常は見当たらない。

**原因の切り分け**: 2026-08-14・2026-08-27の改善提案で既に特定済みの「GitHub側 schedule
トリガーの遅延・間引き」と同じ系統。`concurrency: cancel-in-progress: false` の pending
キューはcronが発火して初めて機能するため、**cronそのものが発火しない**今回のケースには効かない。
`config.json` / `monitor.yml` / `hermes_monitor.py` に今回特有の不具合は見当たらない。

**試みた対応**: 自動点検セッションから `workflow_dispatch` での手動再起動を試みたが、
`403 Resource not accessible by integration` でこのセッションのGitHub権限では実行できず、
断念した（ワークフロー自体は手動起動に対応しており、権限があれば人間が再開できる）。
制約の範囲で安全なコード修正も無いため、コードは変更していない。

**この件が過去2回と異なる点**: 2026-08-14・2026-08-27はいずれも点検時点で既に自己解消していたが、
今回は**点検時点で進行中かつ拡大中**（監視の空白がリアルタイムで伸びている）。また
2026-08-14→08-27→08-28と**3日連続**で同系統の劣化が発生しており、頻度が上がっている。
2026-08-14の改善提案に記載済みの「常駐実行への移行」（Oracle Cloud Always Free / Fly.io 等で
`--loop` を回し、GitHub Actionsは死活監視に格下げする案）を、次回の日次点検までに劣化が
解消しない場合は本格検討することを推奨する。

### 2026-08-29: cron間隔劣化が解消せず、直近24時間で2回発生（4日連続）

**確認した事実**（直近24時間＝2026-08-28 12:34〜2026-08-29 12:34 UTC、GitHub Actions run一覧と
`logs/freshness.jsonl` を突合）:

- 直近24時間の run は3件（#334, #335, #336）で、failure/timed_out/cancelled は0件。ScrapFly経由
  比率は96.6%（58件中56件）で健全。個々のrun自体は所要165〜166分（予算170分以内）で正常完走。
- 一方でrun開始時刻の間隔は #334→#335 が451分、#335→#336 が411分（いずれも45分の異常しきい値・
  設計上の連続監視を大きく超過）。`logs/freshness.jsonl` の `checked_at` ギャップも同期間で最大286分
  （2026-08-28 21:21→2026-08-29 02:07 UTC）。両区間とも run 一覧に該当する run が1件も
  （cancelledですら）存在せず、2026-08-14/08-27/08-28で特定済みの「GitHub側 scheduleトリガーの
  遅延・間引き」と同じ症状。`config.json` / `monitor.yml` / `hermes_monitor.py` に該当期間の
  コード変更は無く、今回特有の不具合も見当たらない。

**08-28の記載との違い**: 08-28は単発（点検時点で進行中の1件）だったが、今回は直近24時間の
run間隔3区間のうち**2区間が同種の劣化**（451分・411分）で、実質「170分監視→400分超の空白」が
定常化しつつある。24時間あたりの実カバレッジは 約165分×3回／1440分 ≈ **35%** で、2026-08-14の
対策前（設計の19%）に近い水準まで低下している。

**対応**: 制約（監視間隔を狭めない・大規模な設計変更をしない）の範囲で安全に直せるコード修正は
今回も無く、コードは変更していない。08-28の改善提案が設定した「次回の日次点検までに劣化が解消
しない場合は本格検討する」という条件に該当したため、2026-08-14の改善提案に記載済みの選択肢
（常駐実行への移行、state変化時commit化によるrun長延長、外部cronサービスからの`workflow_dispatch`
起動）の本格検討を、次の対応として推奨する。自動点検の範囲（コード変更なし・依存追加なし）を
超える判断のため、この場では実施しない。

### 2026-08-30: cron間隔劣化が5日連続だが、規模は直近3日で最小（新規対応なし）

**確認した事実**（直近24時間＝2026-08-29 12:34〜2026-08-30 12:34 UTC、GitHub Actions run一覧と
`logs/freshness.jsonl` を突合）:

- 直近24時間の run は5件（#337〜#341）で、failure/timed_out/cancelled は0件。ScrapFly経由比率は
  116件中116件（100%）で健全。個々の run 自体も正常完走。
- 一方で run 開始間隔は239分・197分・154分・380分で、最大は #340→#341 の380分（45分の異常
  しきい値・170分の設計を超過）。`logs/freshness.jsonl` の `checked_at` ギャップも同区間で最大214分
  （2026-08-30 03:02→06:36 UTC）。2026-08-14/08-27/08-28/08-29で特定済みの「GitHub側 schedule
  トリガーの遅延・間引き」と同じ症状で5日連続の発生。ただし今回の最大ギャップ（380分／214分）は
  08-28（372分）・08-29（451分）より小さく、直近3日では最も軽微。`config.json` / `monitor.yml` /
  `hermes_monitor.py` に該当期間のコード変更はなく、今回特有の不具合も見当たらない。

**対応**: 08-29の改善提案で既に「常駐実行への移行等の本格検討」を次の対応として推奨済みであり、
今回の観測（症状は継続・規模はむしろ縮小）はその判断を変えるものではないため、新たな提案の追加や
自動点検の範囲を超える対応は行っていない。コードは変更していない。

### 2026-08-31: cron間隔劣化が6日連続（新規対応なし）

**確認した事実**（直近24時間＝2026-08-30 12:36〜2026-08-31 12:36 UTC、GitHub Actions run一覧と
`logs/freshness.jsonl` を突合）:

- 直近24時間の run は5件（#342〜#346）で、failure/timed_out/cancelled は0件。ScrapFly経由比率は
  110件中107件（97.3%）で健全。個々の run 自体も正常完走。
- 一方で run 開始間隔は275分・215分・147分・359分で、45分の異常しきい値・170分の設計を超過。
  `logs/freshness.jsonl` の `checked_at` ギャップも同期間で最大174.5分（2026-08-31 11:31→14:26 JST）。
  さらに点検時点（12:36 UTC）で run #346（05:25〜08:11 UTC完走）以降、次の run が
  1件も（cancelledですら）発生しておらず、`checked_at` も08:11 UTCで止まったまま
  約4時間25分（延伸中）。2026-08-14/08-27/08-28/08-29/08-30で特定済みの「GitHub側 schedule
  トリガーの遅延・間引き」と同じ症状で6日連続の発生。`config.json` / `monitor.yml` /
  `hermes_monitor.py` に該当期間のコード変更はなく（直近の変更は08-23の初期コミットのみ）、
  今回特有の不具合も見当たらない。

**対応**: 08-29の改善提案で既に「常駐実行への移行等の本格検討」を次の対応として推奨済みであり、
今回の観測（症状は継続、規模も過去数日と同水準）はその判断を変えるものではないため、新たな提案の
追加や自動点検の範囲を超える対応は行っていない。コードは変更していない。

### 2026-09-01: cron間隔劣化が7日連続（新規対応なし）

**確認した事実**（直近24時間＝2026-08-31 12:36〜2026-09-01 12:36 UTC、GitHub Actions run一覧と
`logs/freshness.jsonl` を突合）:

- 直近24時間の run は5件（#347〜#351）で、failure/timed_out/cancelled は0件。#351は点検時点で
  in_progress（開始から約154分経過、170分ループ設計の範囲内）。完走した4件（#347〜#350）は
  すべて成功、ScrapFly経由比率は点検時点で取得できた範囲（82件中80件）で97.6%と健全。
- 一方で run 開始間隔は382分（#347→#348）・228分（#348→#349）・312分（#349→#350）・
  302分（#350→#351、in_progress時点までの経過）で、45分の異常しきい値・170分の設計を
  いずれも超過。`logs/freshness.jsonl` の `checked_at` ギャップも同期間で最大217.4分
  （2026-08-31 16:22→19:59 UTC）。2026-08-14/08-27/08-28/08-29/08-30/08-31で特定済みの
  「GitHub側 scheduleトリガーの遅延・間引き」と同じ症状で7日連続の発生。`config.json` /
  `monitor.yml` / `hermes_monitor.py` に該当期間のコード変更はなく（直近の変更は08-23の
  初期コミットのみ）、今回特有の不具合も見当たらない。

**対応**: 08-29の改善提案で既に「常駐実行への移行等の本格検討」を次の対応として推奨済みであり、
今回の観測（症状は継続・規模も過去数日と同水準）はその判断を変えるものではないため、新たな提案の
追加や自動点検の範囲を超える対応は行っていない。コードは変更していない。

### 2026-09-02: cron間隔劣化が8日連続（新規対応なし）

**確認した事実**（直近24時間＝2026-09-01 12:36〜2026-09-02 12:36 UTC、GitHub Actions run一覧と
`logs/freshness.jsonl` を突合）:

- 直近24時間の run は6件（#352〜#357）で、すべて成功・failure/timed_out/cancelled は0件。
  ScrapFly経由比率は133件中124件（93.2%）で健全。個々の run 自体も正常完走（所要145〜166分、
  170分予算内）。
- 一方で run 開始間隔は217分（#352→#353）・198分（#353→#354）・141分（#354→#355）・
  258分（#355→#356）・309分（#356→#357）で、45分の異常しきい値・170分の設計をいずれも
  超過。`logs/freshness.jsonl` の `checked_at` ギャップも同期間で最大144.4分
  （2026-09-02 07:08→09:32 UTC、ちょうど#356→#357の間）。2026-08-14/08-27〜09-01で
  特定済みの「GitHub側 scheduleトリガーの遅延・間引き」と同じ症状で8日連続の発生。
  `config.json` / `monitor.yml` / `hermes_monitor.py` に該当期間のコード変更はなく（直近の
  変更は08-23の初期コミットのみ）、今回特有の不具合も見当たらない。今回の最大ギャップ
  （309分／144.4分）は09-01（382分／217.4分）よりやや軽微。

**対応**: 08-29の改善提案で既に「常駐実行への移行等の本格検討」を次の対応として推奨済みであり、
今回の観測（症状は継続・規模も過去数日と同水準かやや軽微）はその判断を変えるものではないため、
新たな提案の追加や自動点検の範囲を超える対応は行っていない。コードは変更していない。

### 2026-09-03: cron間隔劣化が9日連続（新規対応なし）

**確認した事実**（直近24時間＝2026-09-02 12:34〜2026-09-03 12:34 UTC、GitHub Actions run一覧と
`logs/freshness.jsonl` を突合）:

- 直近24時間の run は6件（#358〜#363）で、すべて成功・failure/timed_out/cancelled は0件。
  ScrapFly経由比率は128件中122件（95.3%）で健全。個々の run 自体も正常完走（所要145〜166分、
  170分予算内）。
- 一方で run 開始間隔は252分（#358→#359）・188分（#359→#360）・144分（#360→#361）・
  249分（#361→#362）・324分（#362→#363）で、45分の異常しきい値・170分の設計をいずれも
  超過。`logs/freshness.jsonl` の `checked_at` ギャップも同期間で最大158.8分
  （2026-09-03 07:03→09:42 UTC、#362→#363の間に対応）。2026-08-14/08-27〜09-02で
  特定済みの「GitHub側 scheduleトリガーの遅延・間引き」と同じ症状で9日連続の発生。
  `config.json` / `monitor.yml` / `hermes_monitor.py` に該当期間のコード変更はなく（直近の
  変更は08-23のガーデンパーティ修正のみ）、今回特有の不具合も見当たらない。今回の最大ギャップ
  （324分／158.8分）は09-02（309分／144.4分）よりやや悪化だが、09-01（382分／217.4分）・
  08-31（359分／174.5分）よりは軽微で、この2週間の変動幅の範囲内。

**対応**: 08-29の改善提案で既に「常駐実行への移行等の本格検討」を次の対応として推奨済みであり、
今回の観測（症状は継続・規模はこれまでの変動幅の範囲内）はその判断を変えるものではないため、
新たな提案の追加や自動点検の範囲を超える対応は行っていない。コードは変更していない。

### 2026-09-04: cron間隔劣化が10日連続（新規対応なし）

**確認した事実**（直近24時間＝2026-09-03 12:35〜2026-09-04 12:35 UTC、GitHub Actions run一覧と
`logs/freshness.jsonl` を突合）:

- 直近24時間の run は6件（#364〜#369）で、すべて成功・failure/timed_out/cancelled は0件。
  ScrapFly経由比率は128件中124件（96.9%）で健全。個々の run 自体も正常完走。
- 一方で run 開始間隔は246分（#364→#365）・186分（#365→#366）・141分（#366→#367）・
  262分（#367→#368）・310分（#368→#369）で、45分の異常しきい値・170分の設計をいずれも
  超過。`logs/freshness.jsonl` の `checked_at` ギャップも同期間で最大145.1分
  （2026-09-04 07:08→09:33 UTC、#368→#369の間に対応）。2026-08-14/08-27〜09-03で
  特定済みの「GitHub側 scheduleトリガーの遅延・間引き」と同じ症状で10日連続の発生。
  `config.json` / `monitor.yml` / `hermes_monitor.py` に該当期間のコード変更はなく、
  今回特有の不具合も見当たらない。今回の最大ギャップ（310分／145.1分）は09-03（324分／
  158.8分）よりやや軽微で、この2週間の変動幅の範囲内。

**対応**: 08-29の改善提案で既に「常駐実行への移行等の本格検討」を次の対応として推奨済みであり、
今回の観測（症状は継続・規模はこれまでの変動幅の範囲内）はその判断を変えるものではないため、
新たな提案の追加や自動点検の範囲を超える対応は行っていない。コードは変更していない。
